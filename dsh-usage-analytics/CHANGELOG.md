# Changelog

## 0.1.0

- Add the durable conversation usage pure projection.
- Register the projection through `ctx.sessionProjections` with reversible fiber ownership.
- Add real Loader composition coverage, the DSH bundle patch, invariant companion, and tarball hygiene checks.
